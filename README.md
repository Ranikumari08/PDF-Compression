Created a PDF compression function in Python with proper UI.

Accept a PDF file as input.

Support multiple compression levels (Low, Medium, High).

Output a compressed PDF based on the selected level.
